import 'package:flutter/material.dart';

class FeacherView extends StatelessWidget {
  const FeacherView({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(body: const Center(child: Text('Home')));
  }
}
